# 実装仕様書: M5Paper 単体サッカー結果掲示板

**構成:** M5Paper 単体（サーバーなし）
**フレームワーク:** Arduino / PlatformIO
**データ源:** API-FOOTBALL（直接呼び出し）
**公開形態:** GitHub Public リポジトリ

**目的:** 欧州サッカー8競技の直近結果を、1日1回の自動更新で e-paper に表示する掲示板。**Ubuntu サーバーも MQTT ブローカーも使わず、M5Paper 単体で完結する。**

---

## 0. このドキュメントの使い方

1. §1 で全体像と設計上のトレードオフを把握する
2. **§2.3（リクエスト予算）と §6.2（電源ラッチ）は単体構成での事故が起きやすい箇所。** 実装前に必ず読む
3. **§7（秘密情報）を先に読む。** 公開リポジトリかつ API キーを扱う
4. §9 のマイルストーン順に実装する

不明点があれば実装前に質問すること。

---

## 1. 設計方針

### 1.1 単体構成で受け入れる制約

サーバー構成（別仕様書 `SPEC_mqtt_eink_board.md`）と比較して、以下を意図的に諦めている。**これらを取り戻そうとしないこと。** 取り戻そうとすると単体構成の利点が消える。

| 失うもの | 代替 |
|---|---|
| サーバー側レンダリング（自動フィット） | デバイス側で防御的にレイアウトする（§5.3） |
| Claude API によるプチ情報の自然な言い回し | 定型テンプレートで機械的に生成する（§4.2） |
| 手動更新が API を消費しない性質 | 日次リクエスト上限で防御する（§2.3） |
| サーバー側でのログ・デバッグ | microSD にログを書く（§8.2） |

### 1.2 得るもの

- Ubuntu、Mosquitto、systemd、Python が一切不要
- 電源とWi-Fiがあればどこでも動く
- リポジトリが1プロジェクトで完結する

### 1.3 フレームワークの選択

**Arduino（PlatformIO）+ M5Unified / M5GFX を使う。ESP-IDF ではない。**

理由:
- **M5GFX の VLW フォント読み込み**（§5.4）が日本語表示の要になる。ESP-IDF で同等のことをやると、フォント基盤の実装から必要になる
- ArduinoJson のフィルタ付きストリーミングパース（§3.2）がこの構成の中核で、Arduino 前提の資産が厚い
- M5Paper の電源ラッチ・RTC・EPD 描画モードを M5Unified が吸収してくれる

本プロジェクトの価値は §3〜§5 のデータ処理とレイアウトにあり、ドライバ層の再実装ではない。

---

## 2. データ取得

### 2.1 対象競技と優先度

| 優先度 | 競技会 | standings |
|---|---|---|
| 1 | UEFA Champions League | ✓（リーグフェーズ中のみ） |
| 2 | UEFA Europa League | ✓（同上） |
| 3 | Premier League | ✓ |
| 4 | Bundesliga | ✓ |
| 5 | LaLiga | ✓ |
| 6 | FA Cup | — |
| 7 | DFB-Pokal | — |
| 8 | Copa del Rey | — |

優先度は**画面に入りきらない場合の切り捨て順**（§5.2）。カップ戦はノックアウト方式のため順位表が存在しない。

### 2.2 API-FOOTBALL

- 認証は `x-apisports-key` ヘッダ（API-SPORTS 直接接続。RapidAPI は経由しない）
- **無料プランは1日100リクエスト、UTC 00:00 リセット、繰り越しなし**
- `season` パラメータは**シーズン開始年**。2026-27 シーズンなら `season=2026`。年をまたぐ判定のヘルパーを書き、**年末年始の境界を単体テストで検証すること**
- リーグ ID は `/leagues` で解決し、**microSD の `league_ids.json` にキャッシュ**する。毎回叩かない
- 取得範囲は**直近72時間以内に終了した試合**（`FT` / `AET` / `PEN`）

### 2.3 リクエスト予算と日次上限（重要）

```
fixtures   8競技 × 1        =  8
standings  最大5リーグ       = 0〜5
                  1回の取得  = 8〜13
```

**MQTT版との最大の違い: 手動更新が毎回 API を消費する。** サーバー版では手動更新はブローカーの retained を取るだけで API リクエストがゼロだったが、単体構成では押すたびに実 API を叩く。

**以下を必ず実装すること。**

1. **日次リクエストカウンタを microSD に保存する。** UTC 日付が変わったらリセット
2. **1日の取得回数に上限を設ける（`MAX_FETCH_PER_DAY`、既定5回）。** 上限に達した後の手動更新は、通信を行わず固定エリアの再描画のみとする
3. **1回の起床あたりのリクエスト数にも上限（既定20）を設ける。** リトライループでの暴走を防ぐ
4. 429 が返ったらその回の取得を即座に中止する

これらがないと、ボタンを何度か押しただけで昼前に枠を使い切り、翌朝の自動更新が失敗する。

### 2.4 HTTP 接続の効率化（必須）

**13回のリクエストを毎回新しい TLS ハンドシェイクで行わないこと。** ESP32 のハンドシェイクは1回あたり1〜3秒かかり、13回では起床時間が数十秒に膨れてバッテリーを圧迫する。

**`WiFiClientSecure` の接続を維持し、HTTP keep-alive で同一コネクション上に連続してリクエストを流すこと。** ハンドシェイクを1回に抑えれば、起床から電源断まで15秒程度に収まる。

証明書検証は `setCACert()` でルート CA を埋め込むか、証明書バンドルを使う。**`setInsecure()` は使わない。**

### 2.5 前回データの保持

順位変動（`RANK_CHANGE`）と順位帯の出入り（`ZONE_ENTER` / `ZONE_LEAVE`）の判定に前回の順位表が要る。

- 取得した standings の要約（チーム名・順位・form）を **microSD の `standings_prev.json` に保存**する
- 次回取得時に差分を取る
- 初回・ファイル欠損時は差分なしとして扱う（エラーにしない）

---

## 3. JSON パース

### 3.1 レスポンスサイズの問題

API-FOOTBALL の fixtures レスポンスは1リーグで数十KBある。**レスポンス全体を文字列としてバッファしないこと。**

### 3.2 実装方針

**ArduinoJson v7 の `DeserializationOption::Filter` を使い、HTTP ストリームから直接デシリアライズする。**

```cpp
JsonDocument filter;
filter["response"][0]["fixture"]["id"]       = true;
filter["response"][0]["fixture"]["date"]     = true;
filter["response"][0]["fixture"]["status"]["short"] = true;
filter["response"][0]["teams"]["home"]["name"] = true;
filter["response"][0]["teams"]["away"]["name"] = true;
filter["response"][0]["goals"]["home"]        = true;
filter["response"][0]["goals"]["away"]        = true;

deserializeJson(doc, client, DeserializationOption::Filter(filter));
```

- **ArduinoJson のアロケータを PSRAM に向けること。** M5Paper の PSRAM 容量は実機のログで確認する
- フィールド名は実際のレスポンスで必ず検証すること。この仕様書の記載を鵜呑みにしない
- パース失敗・想定外の型・null をすべてハンドルする。**1競技のパースに失敗しても他の競技の処理を続行する**こと
- standings も同様に、`rank` / `team.name` / `form` / `points` のみをフィルタで抽出する

---

## 4. ファクト算出と文言生成

### 4.1 算出するファクト

すべて fixtures と standings（`form` 文字列を含む）から算出する。**追加リクエストは不要。**

| ファクト | 算出方法 | 表示例 |
|---|---|---|
| `WIN_STREAK` | `form` 末尾の連続 W | `3連勝` |
| `LOSS_STREAK` | `form` 末尾の連続 L | `4連敗` |
| `STREAK_BROKEN` | 直前の連敗が今節で途切れた | `連敗脱出` |
| `OPENING_STREAK` | `form` 長 == 節数 かつ 全勝 | `開幕3連勝` |
| `UNBEATEN` | `form` に L がない連続数 | `7試合無敗` |
| `RANK_CHANGE` | 前回順位との差（§2.5） | `3つ順位UP` |
| `ZONE_ENTER` / `ZONE_LEAVE` | 順位帯をまたいだ | `降格圏転落` / `CL圏浮上` |
| `TOP_OF_TABLE` | 順位1位 | `首位` |
| `BIG_WIN` | 得失点差 >= 4 | `大勝` |
| `UPSET` | 順位差 >= 8 で下位が勝利 | `番狂わせ` |

**順位帯の定義（降格圏・CL圏など）はリーグごとに異なる。** microSD の `competitions.json` に外出しし、ハードコードしないこと。

```json
{
  "premier_league": {
    "total_teams": 20,
    "zones": [
      { "name": "首位",   "from": 1,  "to": 1  },
      { "name": "CL圏",   "from": 1,  "to": 4  },
      { "name": "降格圏", "from": 18, "to": 20 }
    ]
  }
}
```

### 4.2 文言生成

**LLM は使わない。定型テンプレートで機械的に生成する。**

```c
// 例
case WIN_STREAK:     snprintf(buf, n, "%d連勝", f->count); break;
case OPENING_STREAK: snprintf(buf, n, "開幕%d連勝", f->count); break;
case ZONE_ENTER:     snprintf(buf, n, "%s転落", f->zone_name); break;
```

- テンプレートは**1ファイルに集約**し、後から書き換えやすくすること
- **1試合につき表示するファクトは最大1件。** 優先度をファクト種別ごとに定義し、最も価値の高いものを選ぶ（`OPENING_STREAK` > `ZONE_ENTER` > `WIN_STREAK` > `BIG_WIN` > ...）
- ファクトがない試合はスコアのみを表示する。**無理に何か書かない**

### 4.3 ファクト算出は純粋関数にする

`facts.cpp` はネットワークにも描画にも依存させない。**単体テストを書くこと**（PlatformIO の `test/` でネイティブ実行）。

特に検証すべきケース:
- `form` 文字列の向き（末尾が最新かどうかを実データで確認する）
- 引き分け `D` の扱い
- シーズン序盤で `form` が短い場合
- 順位帯の境界値（17位と18位）
- 前回データが存在しない初回

---

## 5. 画面

### 5.1 レイアウト

M5Paper 960 × 540（横向き）。

```
┌────────────────────────────────────────────────────────────┐
│ 更新 8/29 07:00   26.4℃ 58%  🔋72%   更新→側面ボタン         │ ← 固定エリア 70px
├────────────────────────────────────────────────────────────┤
│ ■ Premier League                                           │ ← 競技会見出し
│   Arsenal            2 - 1  Chelsea          開幕3連勝       │
│   Liverpool          0 - 3  Everton          番狂わせ        │
│                                                            │
│ ■ Bundesliga                                               │
│   Bayern München     4 - 0  Union Berlin     首位           │
│                                                            │
│ ■ LaLiga                                                   │
│   Real Sociedad      1 - 1  Sevilla                        │
│   Alavés             0 - 2  Girona           降格圏転落      │
│                                                            │
│                                          ほか 6試合          │ ← 溢れ表示
└────────────────────────────────────────────────────────────┘
```

**チーム名・競技会名は原語表記のまま。** 日本語化しない。プチ情報のみ日本語（漢字可、大人向け）。

### 5.2 選抜と溢れ処理

```
1. 全試合を優先度スコアで降順ソート
     第1キー: §2.1 の競技会優先度
     第2キー: キックオフ時刻の新しい順
2. 上から順に積み、画面下端に達したら打ち切る
3. 末尾に「ほか ○試合」と表示
```

競技会見出しの行も高さを消費するので、**見出しだけ描いて試合が1件も入らない状態にならないよう**、見出しと最初の1試合をセットで判定すること。

### 5.3 防御的レイアウト（単体構成の要）

サーバー側レンダリングがないため、**自動フィットが効かない。** 溢れをコードで防ぐ。

- **チーム名は必ず `textWidth()` で実測し、上限幅を超えたら切り詰めて末尾に `…` を付ける。** `Borussia Mönchengladbach` のような長い名前が普通に来る
- 切り詰めは**単純な文字数ではなくピクセル幅で判定する。** 原語表記の半角英字と日本語の全角が混在するため、文字数は幅の代理指標にならない
- ホーム名・スコア・アウェイ名・ファクトの4カラムは**固定幅**とし、各カラム内で個別に切り詰める
- 画面下端の判定は描画前に行い、**はみ出す行は描かない**（描いてから消さない）

### 5.4 日本語フォント

**VLW 形式のフォントファイルを microSD に置き、M5GFX の `loadFont()` で読み込む。** ファームウェアに埋め込まない。

- **必要な文字は日本語150字程度 + Latin-1 全域。** ドイツ語のウムラウト（`Mönchengladbach`）とスペイン語のアクセント（`Alavés`）が出るため、Latin-1 Supplement を必ず含める
- サイズは 20px / 28px の2種類
- **サブセット化した VLW 生成スクリプトをリポジトリに含めること。** §4.2 のテンプレートを変更したら文字リストも更新する運用を README に書く
- ライセンスは SIL OFL のフォントを使い、**OFL 全文を同梱する**（§7.3）
- **SD が読めない場合のフォールバック**を必ず実装する。日本語なしの英数字のみでも画面が成立するようにし、固定エリアに `FONT ERR` を表示する。SD の抜き差しや故障で完全に沈黙するのは避ける

### 5.5 EPD 更新戦略

| 状況 | モード |
|---|---|
| 内容変更時 | 全面書き換え（高品質・フラッシュあり） |
| 固定エリアのみ | 部分書き換え（高速・ゴースト蓄積） |

部分書き換えを10回行ったら強制的に全面書き換えを1回入れてゴーストを消す。カウンタは NVS に保存する。正確なモード名は M5Unified / M5EPD の実装で確認すること。

**取得したデータのハッシュを NVS に保存し、前回と同一なら描画をスキップする。** e-paper の書き換えは電力と寿命を消費する。

---

## 6. ハードウェア

### 6.0 一次情報

1. 公式ドキュメント: https://docs.m5stack.com/ja/core/m5paper
2. `m5stack/M5Unified`（M5Paper 対応の実装）
3. `m5stack/M5EPD`
4. 回路図PDF

PSRAM 容量とハードウェアリビジョンは**実機のログで確認**すること。

### 6.1 使用するペリフェラル

| デバイス | 用途 |
|---|---|
| IT8951 | EPD コントローラ |
| SHT30 | 温湿度（固定エリアに表示） |
| BM8563 | RTC。アラーム起床の要 |
| microSD | 設定・フォント・キャッシュ・ログ |
| 側面物理ボタン | 手動更新 |
| GT911 | **使わない**（§6.3） |

### 6.2 電源ラッチと起床（最大の罠）

**M5Paper は ESP32 の deep sleep ではなく、電源ラッチ回路で完全に電源を切れる。** BM8563 の RTC アラームが電源を再投入する。

**必ず理解しておくべき帰結:**

1. **起床は電源投入によるコールドブートであり、スリープ復帰ではない。** RAM も RTC メモリも保持されない。持ち越したい状態はすべて **NVS または microSD** に書く
2. **USB 給電中は電源断が正しく動作しない。** 給電されているとラッチを切っても落ちない、あるいは即座に再起動する。**開発中に「電源が切れない」ように見えるのは正常。** バッテリー駆動でのみ本来の動作をする。README に必ず明記すること
3. 開発中は電源断の代わりにログ出力＋長い delay に切り替えられる `#define DEV_NO_POWEROFF` を用意する

**電源断前に必ず行うこと:**

- BM8563 に次回アラームを設定する。**設定に失敗したら電源を切らない**（二度と起きなくなる）
- NVS / SD への書き込みを完了させる
- IT8951 をスリープさせる

### 6.3 更新操作

**電源が切れている間、GT911 のタッチは反応しない。** 画面にソフトボタンを描いてもタップでは何も起きない。電源ラッチ方式の必然的な代償である。

**「更新」は側面の物理ボタンに割り当てる。** 正確な部品構成と挙動は §6.0 の一次情報で確認すること。

固定エリアに常時案内を出す。

```
更新 → 側面ボタン
```

**押されたら、通信を始める前に固定エリアを部分書き換えして「更新中…」を表示すること。** 通信に10秒以上かかるので、無反応に見えると連打される。部分書き換えは1秒以内に反映できる。

### 6.4 更新頻度とクールダウン

- **自動更新は1日1回、日本時間 07:00**（`daily_wake_local`、SD の設定で変更可能）
- 欧州の遅い試合（CL・LaLiga の最終枠）は日本時間 06:00〜07:00 頃に終わるため、間に合わないことがある。**取得範囲が72時間なので、漏れた試合は翌日に載る。データは失われない**
- 手動更新のクールダウンは `MIN_REFRESH_SEC`（既定600秒）。この間の手動起床は通信せず、固定エリアに `更新済み` を表示して電源断
- **日次リクエスト上限（§2.3）に達している場合も通信せず、`本日の取得上限に達しました` を表示する**
- **手動起床の場合も、次回アラームは翌日の定時に設定する。** 現在時刻 + 24時間としないこと

### 6.5 起動シーケンス

```
電源投入（RTCアラーム または 物理ボタン）
  → 起床理由の判定
  → NVS / SD 読み出し（設定・前回ハッシュ・日次カウンタ・前回順位表）
  → SHT30 測定
  → 固定エリアを部分書き換え「更新中…」
  → 電池電圧チェック（低ければ通信せずアラーム設定して電源断）
  → クールダウン判定 / 日次上限判定 → 該当すれば通信せず終了処理へ
  → Wi-Fi 接続（タイムアウト20秒）
       失敗 → SD にログ、アラーム設定して電源断（画面は前回のまま）
  → SNTP 同期 → RTC 書き戻し
  → HTTPS 接続（keep-alive で維持、§2.4）
       → fixtures 8競技
       → 変化のあったリーグの standings
  → ファクト算出 → 選抜 → ハッシュ比較
       同一 → 固定エリアのみ更新
       相違 → 全面描画
  → SD へキャッシュ・ログ・カウンタを保存
  → 次回アラーム設定 → 電源断
```

**目標: 起床から電源断まで20秒以内。**

---

## 7. 設定と秘密情報

### 7.1 秘密情報を microSD に置く（重要）

**Wi-Fi 資格情報と API キーをファームウェアにコンパイルしないこと。**

**microSD 上の `config.json` から実行時に読む。**

```json
{
  "wifi_ssid": "...",
  "wifi_password": "...",
  "apisports_key": "...",
  "daily_wake_local": "07:00",
  "max_fetch_per_day": 5,
  "min_refresh_sec": 600
}
```

この方式には3つの利点がある。

1. **ファームウェアが秘密情報を含まないので、そのまま公開できる**
2. Wi-Fi や API キーの変更に**再書き込みが不要**
3. ビルド成果物を他人に渡しても資格情報が漏れない

**`config.json` は必ず `.gitignore` に入れ、`config.example.json` のみをコミットする。**

SD が読めない、または `config.json` が無い場合は、画面に `SD CONFIG ERROR` と設定手順の要約を表示して停止すること。無言で失敗しない。

### 7.2 .gitignore

```
.pio/
sd/config.json
sd/cache/
sd/logs/
*.vlw
```

### 7.3 ライセンスと帰属表示

- リポジトリは MIT。`LICENSE` を置く
- `NOTICE.md` に以下を明記
  - **API-FOOTBALL (API-SPORTS):** 利用規約と帰属表示の要否を確認すること。**無料プランには表示条件が付く場合がある**ので、公開前に必ず規約を読む
  - **フォント:** SIL OFL 1.1。ライセンス全文を同梱。派生フォントのファイル名に元の書体名をそのまま使わない
  - M5Unified / M5GFX、ArduinoJson のライセンス

---

## 8. 品質要件

### 8.1 堅牢性

- **取得失敗時は前回の画面をそのまま残す。** e-paper なのでこれは劣化ではない。画面をクリアしたりエラー画面で埋めたりしない
- 固定エリアの更新時刻表示を `失敗 HH:MM` に変えて、最終成功時刻が分かるようにする
- 1競技の取得・パースに失敗しても、残りの競技の処理を続行する
- 連続失敗回数を NVS に記録し、閾値（既定5回）を超えたら起床間隔を延ばして電池を温存する
- **アラーム設定の失敗時のみ電源を切らない**（§6.2）

### 8.2 ログ

**サーバーがないためログの置き場が microSD しかない。** 単体構成でのデバッグ手段はこれだけなので、必ず実装する。

- `sd/logs/YYYY-MM-DD.log` に追記
- 記録内容: 起床理由、消費リクエスト数、HTTP ステータス、パースした試合数、算出したファクト数、描画したかスキップしたか、起床から電源断までの所要時間、空きヒープ
- **APIキーは必ずマスクする**
- ログファイルは世代管理し、古いものを削除する（SD を埋めない）

---

## 9. 成果物とマイルストーン

### 9.1 リポジトリ構成

```
m5paper-football-board/
├── README.md
├── LICENSE
├── NOTICE.md
├── .gitignore
├── platformio.ini
├── .github/workflows/build.yml   # PlatformIO ビルド確認
├── src/
│   ├── main.cpp                  # 起動シーケンス（§6.5）
│   ├── config.cpp/.h             # SD からの設定読み込み（§7.1）
│   ├── api_football.cpp/.h       # 取得とフィルタパース（§2, §3）
│   ├── facts.cpp/.h              # ファクト算出（§4・純粋関数）
│   ├── messages.h                # 文言テンプレート（§4.2）
│   ├── selector.cpp/.h           # 選抜と溢れ処理（§5.2）
│   ├── render.cpp/.h             # 描画（§5.3）
│   ├── power.cpp/.h              # RTCアラームと電源断（§6.2）
│   └── storage.cpp/.h            # NVS / SD
├── test/
│   ├── test_facts.cpp
│   └── test_season.cpp           # シーズン年判定の境界（§2.2）
├── tools/
│   └── make_vlw.py               # フォントサブセット生成（§5.4）
└── sd/
    ├── config.example.json
    ├── competitions.json         # 順位帯定義（§4.1）
    └── fonts/OFL.txt
```

### 9.2 マイルストーン

| # | 内容 | 完了条件 |
|---|---|---|
| 1 | プロジェクト雛形 + CI + .gitignore | CI グリーン。`sdkconfig` 相当の秘密が入らない構成 |
| 2 | M5Paper: EPD 点灯とテスト描画 | 画面に矩形が出る |
| 3 | SD 読み込みと `config.json` パース | 設定値がログに出る。SD 無しで `SD CONFIG ERROR` が出る |
| 4 | VLW フォント読み込みと日本語表示 | 「開幕3連勝」と `Mönchengladbach` が両方正しく出る |
| 5 | Wi-Fi + SNTP + RTC + 固定エリア | 時刻と温湿度が表示される |
| 6 | API-FOOTBALL 取得（keep-alive、§2.4） | 8競技分が15秒以内に取得でき、消費数がログに出る |
| 7 | フィルタパース | 試合結果が構造体に入る。1競技失敗しても続行する |
| 8 | facts.cpp + 単体テスト | form パースと順位帯の境界が全ケース通過 |
| 9 | 選抜と防御的レイアウト | 長いチーム名が `…` で切り詰められ、はみ出さない |
| 10 | 日次リクエスト上限とクールダウン | 上限到達時に通信せず適切な表示が出る |
| 11 | RTCアラームと電源断 | **バッテリー駆動で** 07:00 に自動起床・更新・電源断が回る |
| 12 | 物理ボタンによる手動更新 | 「更新中…」表示 → 更新 → 翌日定時にアラーム再設定される |
| 13 | ログとエラー処理 | Wi-Fi を切っても前回画面が残り、SD にログが残る |
| 14 | README / NOTICE 完成 | 第三者が clone して構築できる |

**マイルストーン11と12は必ずバッテリー駆動で検証すること**（§6.2）。USB 接続では電源断も物理ボタン起動も本来の挙動にならない。

**マイルストーン6でリクエスト数を必ず計測すること。** ここが想定より多いと、以降のすべての設計（更新頻度・上限値）が崩れる。

---

## 10. スコープ外

- Claude API による文言生成（定型テンプレートで代替。§4.2）
- チーム名の日本語化（原語のまま）
- 「監督交代後の初勝利」等、追加リクエストが必要なファクト
- グレースケール（4bit）描画
- タッチ操作（§6.3）
- OTA 更新
- 複数コンテンツのローテーション
- 順位表そのものの表示（試合結果に絞る）
